import { Typography } from '@mui/material';

export default function AccountPage() {
  return (
    <div>
      <Typography variant="h4" component="h1">
        Account Settings
      </Typography>
      <Typography variant="body1">Manage your account information</Typography>
    </div>
  );
}
